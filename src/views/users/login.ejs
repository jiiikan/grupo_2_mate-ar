<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head") %>

<head>
    <title>Formulario Login</title>
    <link rel="stylesheet" href="/css/acceso.css">
</head>

<body>

	<%- include("../partials/header") %>

    <main class="main">
		<div class="login-container">
		<p class="iniciar">Iniciar sesión</p>
		<form method="POST" class="form-acceso" enctype="multipart/form-data" action="/users/login">
			<label class="login-sub">Correo electrónico</label>
			<input type="text" name="user" placeholder="correo" 
			class="login-input <%= locals.errors && errors.user ? 'is-invalid' : null %>" 
			value= "<%= locals.oldData ? oldData.user : null %>">
			<% if (locals.errors && errors.user) { %>
				<div class="text-danger">
				<%= errors.user.msg %>
			</div>
			<% } %>

			<label class="login-sub">Contraseña</label>
			<input type="password"  name="Contraseña" placeholder="hola81272"
			class="login-input <%= locals.errors && errors.contraseña ? 'is-invalid' : null %>">

			<label class="login-sub"><input type="checkbox" name="recordar">Recordar este usuario</label>

    <div class="bottom">
			<button type="submit" class="boton-iniciar">Iniciar sesión</button>
		</div>
	</form>
		<h3 class="go-register">¿No tenes una cuenta? <a href="/registro"> Regístrate aquí</a></h3>
	</div>
</main>

<%- include("../partials/footer") %>

</body>
</html>